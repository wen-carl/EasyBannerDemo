<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <com.seven.easybanner.EasyBanner
        android:id="@+id/easy_banner"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        app:indicator_style="num_indicator"
        app:auto_play="true"
        app:direction="negative"
        app:image_scale_type="fit_center"
        app:indicator_gravity="end"
        app:indicator_background="@color/gray_30"
        app:indicator_mode="inside"
        app:time_interval="5000"
        app:title_text_color="#ff0000"
        app:indicator_num_text_color="#00ff90"/>

    <LinearLayout
        android:id="@+id/control"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <Button
            android:id="@+id/btn_pause"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_margin="8dp"
            android:text="Pause" />
        <Button
            android:id="@+id/btn_stop"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_margin="8dp"
            android:text="Stop" />
        <Button
            android:id="@+id/btn_reverse"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_margin="8dp"
            android:text="Reverse" />
    </LinearLayout>

    <Spinner
        android:id="@+id/spinner_indicator_style"
        android:layout_width="match_parent"
        android:layout_height="40dp" />

    <Spinner
        android:id="@+id/spinner_transformer"
        android:layout_width="match_parent"
        android:layout_height="40dp" />

</LinearLayout>